# DNSPod 域名记录管理

一个基于 Python + Tkinter 的 DNSPod 域名记录管理工具，输入DNSPod的id和token即可管理。

## 功能
- 增删改查 DNS 记录
- 切换记录状态（启用/禁用）
- 管理记录备注

## 运行环境
- Python 3.x
- requests 库

## 使用方法
1.  `pip install requests`
2.  `python dnspod_manager.py`
